#' Update ETL run entity
update_etl_run <- function(
  run_entity_id,
  entity_status,
  rows_inserted = 0L,
  rows_updated = 0L,
  rows_deleted = 0L,
  error_message = NULL
) {
  stop("Not implemented")
}
