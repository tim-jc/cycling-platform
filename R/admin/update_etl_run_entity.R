#' Update ETL run entity
#'
#' Updates an existing ETL run entity
#' @param run_entity_id ID of existing run entity to update
update_etl_run_entity <- function(
  run_entity_id,
  entity_status,
  rows_inserted = 0L,
  rows_updated = 0L,
  error_message = NULL
) {
  stop("Not implemented")
}
